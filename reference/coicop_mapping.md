# coicop_mapping

Function to map coicop expenditure categories

## Usage

``` r
coicop_mapping(data, mapping_file = mapping_coicop)
```

## Arguments

- data:

  dataframe with the hbs data to rename

- mapping_file:

  mapping file to be used (included in medusa)

## Value

data file with the renamed expenditure categories
